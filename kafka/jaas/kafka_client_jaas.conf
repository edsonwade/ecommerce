// Kafka client JAAS config — used by Spring Boot services
// Values are injected at runtime via KAFKA_OPTS environment variable

KafkaClient {
  org.apache.kafka.common.security.plain.PlainLoginModule required
  username="${KAFKA_USERNAME}"
  password="${KAFKA_PASSWORD}";
};
